packages:
  - "apps/*"
  - "packages/*"
allowBuilds:
  esbuild: true

# GHSA-5j98-mcp5-4vw2: glob 11.0.x (transitivo via workbox-build) — força a versão corrigida
overrides:
  "glob@>=11.0.0 <11.1.0": ">=11.1.0"
